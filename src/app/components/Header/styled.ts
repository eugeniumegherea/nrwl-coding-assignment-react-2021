import styled from 'styled-components';

export const Container = styled.nav`
  height: 56px;
  background: #3f51b5;
  color: #fff;
  padding: 0 16px;
`;

export const Title = styled.span`
  font-weight: 600;
  font-size: 18px;
  line-height: 56px;
`;